git clone <link>

DOWNLOAD:
NPM version 20
VSCode
git

*Don't forget to set the environment variables in case it's not set in the system. 

AFTER MA CLONE:
npm install
npm install lucide-vue-next
npm install supabase OR npm install @supabase supabase-js
npm install xslx

npm install browser-image-compression
npm install jszip

npm install jspdf jspdf-autotable html2canvas

